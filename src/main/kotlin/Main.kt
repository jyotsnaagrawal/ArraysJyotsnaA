fun main() {
    // List of Variables
    val supplies = arrayOf("Hot Dogs", "Buns", "Ketchup", "Mustard", "Onions")

    // Use a for loop to print the contents of the array
    for (supply in supplies) {
        println(supply)
    }
}
